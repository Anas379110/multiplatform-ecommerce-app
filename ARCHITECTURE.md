# 🏗️ المعمارية — تطبيق تجارة إلكترونية متعدد المنصات

## نظرة عامة على المعمارية
منشور على Google Play وApp Store بتقييمات عالية. المعمارية مبنية بشكل يضمن قابلية التوسع (Scalability) والصيانة (Maintainability) على المدى الطويل.

## المكوّنات الرئيسية
- **تطبيق الموبايل:** بُني بـ Flutter بقاعدة كود واحدة لنظامي iOS وAndroid، مع فصل واضح بين طبقة العرض (UI) وطبقة الحالة (State Management).
- **طبقة الاتصال بالخلفية:** REST API (و WebSocket للميزات اللحظية) عبر طبقة Repository موحّدة تُخفي تفاصيل الشبكة عن باقي التطبيق.
- **التخزين المحلي:** ذاكرة تخزين محلية على الجهاز (Local Cache) لدعم العمل جزئياً بدون اتصال ومزامنة التغييرات عند عودة الاتصال.
- **الإشعارات:** إشعارات Push مربوطة بخدمة مركزية لإعلام المستخدم بالتحديثات المهمة حتى مع إغلاق التطبيق.
- **التكاملات الخاصة بالجهاز:** Push Notifications عبر Platform Channels عند الحاجة.

## تدفّق البيانات (Data Flow)
التطبيق يرسل الطلبات عبر طبقة Repository إلى الـ API الخلفي → البيانات تُخزَّن محلياً لعرض فوري وحتى في حال ضعف الاتصال → أي تغيير يُزامَن مع الخادم فور توفر الاتصال.

## قابلية التوسّع (Scalability)
النظام مصمم للتوسّع الأفقي (Horizontal Scaling) — يمكن تشغيل عدة نسخ من كل مكوّن خلف موازن حمولة (Load Balancer) دون تغيير في منطق العمل، وتمت مراعاة ذلك منذ مرحلة التصميم الأولى بدل تركه لمرحلة لاحقة.

## المرونة تجاه الأعطال (Resilience)
تم اعتماد مبدأ الفشل الآمن (Fail Gracefully): مهلات زمنية (Timeouts) محددة لكل استدعاء خارجي، إعادة محاولة (Retry) محدودة مع تأخير تصاعدي (Exponential Backoff)، وحدود قاطعة (Circuit Breaker) لمنع انهيار متسلسل بين الخدمات المترابطة.

---
*جزء من [توثيق المشروع الكامل](./PROJECT.md)*
